const state = {
    title: ''
};

const getters = {

};

const actions = {

};

const mutations = {
    setTitle: (state, newValue) => {
        state.title = newValue
    }
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
